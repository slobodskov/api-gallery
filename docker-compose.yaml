version: '3.8'

services:
  photo-gallery:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./gallery.db:/app/gallery.db
      - ./.env:/app/.env
    environment:
      - GIN_MODE=release