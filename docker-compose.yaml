version: '3.8'

services:
  photo-gallery:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./gallery.db:/app/gallery.db
    environment:
      - GIN_MODE=release