# Photo Gallery API

REST API для управления фотогалереей с возможностью загрузки, просмотра и удаления фотографий.

## Функциональность

- 📸 Загрузка фотографий с автоматическим созданием превью
- 👀 Просмотр списка всех фотографий
- 🗑️ Удаление фотографий
- 📊 Информация о размере и разрешении фотографий
- 🖼️ Генерация превью изображений

## Технологии

- **Go 1.25** - основной язык программирования
- **Gin** - веб-фреймворк
- **SQLite** - база данных
- **Docker** - контейнеризация
- **Swagger** - документация API

## Запуск приложения

### Локальная разработка

1. Клонируйте репозиторий:

    git clone <repository-url>

    cd api-gallery

2. Установите зависимости:

    go mod download

3. Запустите:

   go run main.go

### Запуск в Docker

1. Соберите и запустите контейнер:

   docker-compose up --build
2. Приложение будет доступно по адресу: http://localhost:8080


## API Endpoints

### Загрузка фотографии
POST /api/photos
Content-Type: multipart/form-data

### Получение списка фотографий
GET /api/photos

### Удаление фотографии
DELETE /api/photos/:id

### Получение превью
GET /api/photos/preview/:filename

## Конфигурация
Настройки приложения задаются через переменные окружения:

DB_PATH - путь к файлу базы данных (по умолчанию: ./gallery.db)

SERVER_PORT - порт сервера (по умолчанию: 8080)

MAX_FILE_SIZE - максимальный размер файла в байтах (по умолчанию: 10485760 - 10MB)

GIN_MODE - режим работы Gin (по умолчанию: debug)
